{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.game_delete'|trans({'%id%': game.id|default('')}) }}
{% endblock %}

{% block body %}
    {% include 'default/_form.html.twig' with {
        page_title: 'title.game_delete'|trans({'%id%': game.id|default('')}),
        form: form,
        submit_label: 'action.delete'|trans,
        back_to_list_path: 'game_index',
    } %}
{% endblock %}