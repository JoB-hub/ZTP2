{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.platform_details'|trans({'%id%': platform.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.platform_details'|trans({'%id%': platform.id|default('')}) }}</h1>
    {% if platform is defined and platform|length %}
        <dl class="dl-horizontal">
            <dt>{{ 'label.id'|trans }}</dt>
            <dd>{{ platform.id }}</dd>
            <dt>{{ 'label.name'|trans }}</dt>
            <dd>{{  platform.name }}</dd>
        </dl>
        <p>
            <a href="{{ url('platform_index') }}" title="{{ 'action.back_to_list'|trans }}">
                {{ 'action.back_to_list'|trans }}
            </a>
        </p>
    {% else %}
        <p>
            {{ 'message.record_not_found'|trans }}
        </p>
    {% endif %}
{% endblock %}