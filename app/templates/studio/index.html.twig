{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.studio_list'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.studio_list'|trans }}</h1>
    {% if studios is defined and studios|length %}
        <table class="table table-striped">
            <thead>
            <tr>
                <th>{{ 'label.id'|trans }}</th>
                <th>{{ 'label.name'|trans }}</th>
                <th>{{ 'label.actions'|trans }}</th>
            </tr>
            </thead>
            <tbody>
            {% for studio in studios %}
                <tr>
                    <td>{{ studio.id }}</td>
                    <td>{{ studio.name }}</td>
                    <td>
                        <a href="{{ url('studio_show', {id: studio.id}) }}" title="{{ 'action.view'|trans }}">
                            {{ 'action.view'|trans }}
                        </a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% else %}
        <p>
            {{ 'message.empty_list'|trans }}
        </p>
    {% endif %}
{% endblock %}